package com.unddefined.enderechoing.client.renderer.layer;

import com.unddefined.enderechoing.items.WhisperDruse;
import software.bernie.geckolib.renderer.GeoRenderer;
import software.bernie.geckolib.renderer.layer.GeoRenderLayer;

public class WhisperLayer extends GeoRenderLayer<WhisperDruse> {
    public WhisperLayer(GeoRenderer entityRendererIn) {
        super(entityRendererIn);
    }
}
